README

Things left to do:

-Inclusive tag search (i.e. if a bookmark's tag is "tag1 tag2", it should be returned by tag1)

-Links to homepage on add, view pages

-Heroku deployment

-Pretty UI

-Keeping view, add functionality on homepage (might be asynchronous)
